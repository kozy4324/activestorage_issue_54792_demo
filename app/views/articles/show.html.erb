<h1><%= @article.title %></h1>

<%= @article.images.attached? %><br/>
<% @article.images.each do |image| %>
    <%= image_tag url_for(image), width: 100 %><br/>
<% end %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>